import 'package:flutter/material.dart';

class TastingNotes extends StatelessWidget {
  const TastingNotes({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
